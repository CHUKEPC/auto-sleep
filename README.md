# Auto Sleep

Автоматический скрипт для перевода компьютера в спящий режим после периода бездействия.

## Описание

Скрипт отслеживает время бездействия пользователя и автоматически переводит систему в спящий режим, если пользователь не взаимодействовал с компьютером в течение заданного времени (по умолчанию 30 минут).

## Возможности

- ✅ Отслеживание времени бездействия системы
- ✅ Автоматический переход в спящий режим
- ✅ Настраиваемые параметры (время простоя, интервал проверки)
- ✅ Работает в фоновом режиме

## Требования

- Windows (использует Windows API)
- Python 3.6+

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/CHUKEPC/auto-sleep.git
cd auto-sleep
```

2. Убедитесь, что у вас установлен Python 3.6 или выше.

## Использование

Запустите скрипт:

```bash
python main.py
```

Скрипт будет работать в фоновом режиме, проверяя время бездействия каждую минуту. Когда время простоя достигнет 30 минут, система автоматически перейдет в спящий режим.

## Настройка

Вы можете изменить параметры в файле `main.py`:

- `IDLE_THRESHOLD_SECONDS` - время простоя в секундах перед переходом в спящий режим (по умолчанию: 1800 секунд = 30 минут)
- `CHECK_INTERVAL` - интервал проверки времени простоя в секундах (по умолчанию: 60 секунд = 1 минута)

## Пример

```python
IDLE_THRESHOLD_SECONDS = 1800  # 30 минут
CHECK_INTERVAL = 60            # Проверка каждую минуту
```

## Примечания

- Скрипт работает только на Windows
- Для работы скрипта требуются права администратора
- Для остановки скрипта используйте `Ctrl+C`

## Альтернативные варианты

В коде есть закомментированная строка для выключения компьютера вместо перехода в спящий режим:

```python
# os.system("shutdown /s /t 1")
```

Раскомментируйте эту строку и закомментируйте строку с `SetSuspendState`, если хотите выключать компьютер вместо перехода в спящий режим.

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для подробностей.

## 👤 Автор

**CHUKEPC**
- GitHub: [@CHUKEPC](https://github.com/CHUKEPC)
- Telegram: [@ArtyomRoyal](https://t.me/ArtyomRoyal)

---

<div align="center">

⭐ Если проект был полезен, поставьте звезду! ⭐

</div>